SELECT MAX(DATETIME) FROM ANIMAL_INS

-- SQL MAX(), MIN() 함수 -- 
 1. MAX()
MAX() 은 선택된 칼럼의 가장 큰 값을 가져옵니다.
기본 구문 : SELECT MAX(column_name) FROM table_name;
2. MIN()
MIN() 은 선택된 칼럼의 가장 작은 값을 가져옵니다.
기본 구문 : SELECT MIN(column_name) FROM table_name;